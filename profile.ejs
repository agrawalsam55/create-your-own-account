<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">
          <a href="/logout" class="bg-red-500 rounded-md px-3 py-2">Logout</a>  
        </div>
        <div class="flex gap-2 items-start">
            <div class="w-11 h-11 rounded-[50%] overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-4xl"> <span class="font-light">Hello</span>, <%= user.name %> ☺️</h3>
        </div>
        <h5 class="mb-7 mt-3">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea placeholder="What's on your mind.. ?" name="content" class="w-1/3 resize-none bg-transparent border-2 bg-zinc-800 rounded-md outline-none p-3 text-2xl block"></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 block mt-5 rounded-md" type="submit" value="Create New Post">
        </form>


        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>

            <div class="postcontainer mt-5">
               <% user.posts.reverse().forEach(function(post){ %>
                <div class="post mb-4 w-1/3 p-5 border-[1px] bg-zinc-800 rounded-md ">
                    <h4 class="mb-3 text-blue-500">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> like</small>
                    <div class="btns flex mt-4 gap-4">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>

    </div>
</body>
</html> 
