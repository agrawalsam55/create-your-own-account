<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">
          <a href="/logout" class="bg-red-500 rounded-md px-3 py-2">Logout</a>  
        </div>
        <h5 class="mb-7">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="Edit your post" name="content" class="w-1/3 resize-none bg-transparent border-2 bg-zinc-800 rounded-md outline-none p-3 text-2xl block"><%= post.content %></textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 text-black block mt-5 rounded-md" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html> 
